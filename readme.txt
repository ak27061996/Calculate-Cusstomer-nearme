Module to be installed:
 	Express :
 		npm install express --save
 	Customer Coding:
 			npm i customer-coding 


configuration:
	need to set current Point (lattitude and longotude), default set is given point(Dublin)
	need to set file => give file path of customer data , I put customer file in input directory.
	need to set distace (set default 100 which is given in the question)


Run:
	npm start

Output:
	when execute npm start it,output will be on terminal
	with userId,Name,and Distance from Current point(Dublin)


